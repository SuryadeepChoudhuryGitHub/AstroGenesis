# Images Directory (`assets/images/`)

Place general image assets, UI icons, logos, badges, and interface graphics here.

### Supported / Recommended Formats
- PNG (`.png`) - Preferred for transparent UI elements and crisp graphics.
- JPEG (`.jpg`, `.jpeg`) - Suitable for high-density background imagery.
- SVG (`.svg`) - Vector graphics (if using vector renderer).
