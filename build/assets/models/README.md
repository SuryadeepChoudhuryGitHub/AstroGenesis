# Models Directory (`assets/models/`)

Place 3D object and mesh files here.

### Recommended Formats
- `.obj` / `.mtl` (Wavefront OBJ)
- `.gltf` / `.glb` (glTF 2.0 - Recommended)
- `.fbx` (Filmbox format)
